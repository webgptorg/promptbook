import type { TODO_object } from '../../../utils/organization/TODO_object';

/**
 * @@@
 *
 * @@@ `ScraperMetadata` vs `ScraperConfiguration` vs `ScraperOptions` (vs `Registered`)
 */
export type ScraperOptions = TODO_object;

/**
 * TODO: [®] DRY Register logic
 */
