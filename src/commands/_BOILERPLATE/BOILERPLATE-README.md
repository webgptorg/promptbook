# 🚀 Boilerplate command

Boilerplate uses as showcase for creating new commands. It is a simple command that does nothing but print a message.

## Usage

```markdown
-   BOILERPLATE foo
-   BOILERPLATE bar
```
