/**
 * Next production build marker required before a cached build is reused.
 *
 * @private internal constant of `buildAgentsServer`
 */
export const AGENTS_SERVER_NEXT_BUILD_ID_FILENAME = 'BUILD_ID';
