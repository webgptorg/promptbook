import type { number_integer, number_positive } from '../../types/typeAliases';

/**
 * Counts mumber of sentences in the text
 */

export function countSentences(text: string): number_integer & number_positive {
    return 0; /* <- !!!!! Implement */
}
