import type { number_integer, number_positive } from '../../types/typeAliases';

/**
 * Counts mumber of paragraphs in the text
 */

export function countParagraphs(text: string): number_integer & number_positive {
    return 0; /* <- !!!!! Implement */
}
