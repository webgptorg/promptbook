import type { number_integer, number_positive } from '../../types/typeAliases';

/**
 * Counts mumber of pages in the text
 */

export function countPages(text: string): number_integer & number_positive {
    return 0; /* <- !!!!! Implement */
}
