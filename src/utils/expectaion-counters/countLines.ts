import type { number_integer, number_positive } from '../../types/typeAliases';

/**
 * Counts mumber of lines in the text
 */

export function countLines(text: string): number_integer & number_positive {
    return 0; /* <- !!!!! Implement */
}
