/**
 * Describes RGBA channel values that represent a color.
 *
 * @private type of Color
 */
export type ColorChannelSet = {
    red: number;
    green: number;
    blue: number;
    alpha: number;
};
