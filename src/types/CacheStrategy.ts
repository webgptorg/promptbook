/**
 * @@@
 */
export type CacheStrategy = 'HIDE_AND_CLEAN' | 'HIDE_AND_KEEP' /* | 'VISIBLE' <- TODO: [😡] Add */;

/**
 * TODO: [🎅] Maube add options for all kinds of cache NOT only intermediate files
 */
