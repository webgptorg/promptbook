/**
 * Semantic helper
 *
 * For example `"My AI Assistant"`
 *
 * @private internal utility of `string_agent_name.ts`
 */
export type string_agent_name_in_book_private = string;

// <- TODO: [🕛] There should be `agent_fullname` not `string_agent_name_in_book_private`
// <- TODO: [🕛] Search and distinguish between `string_agent_name_private` and `string_agent_name_in_book_private` ACRY + write here in JSDoc difference
