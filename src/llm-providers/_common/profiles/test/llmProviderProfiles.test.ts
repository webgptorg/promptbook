/**
 * (Removed)
 *
 * Legacy test for central LLM provider profiles has been deprecated.
 * Profiles are now colocated with each provider implementation.
 *
 * This placeholder intentionally contains no tests.
 *
 * TODO: Optionally add new tests verifying `.profile` existence on selected providers
 *       without requiring network/API keys (e.g. mocked providers).
 */
export {};
