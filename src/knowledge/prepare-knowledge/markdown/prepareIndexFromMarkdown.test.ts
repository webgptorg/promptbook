import { describe, expect, it } from '@jest/globals';

describe('how creating knowledge from markdown works', () => {
    it('should work with foo', () => expect(Promise.resolve(1)).resolves.toBe(1));
});
