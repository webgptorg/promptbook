[ ]

[✨📜] Create page that can show transpiled / generated code of any agent

-   Create a new page `/agents/[agentName]/code` that displays the generated transpiled code for the specified agent
-   Allow to pick from all available transpilers
-   Show the code with syntax highlighting in monaco editor in read-only mode _(Monaco editor is already used in the project, reuse it)_
-   Link this page from the agent profile page from menu
    -   Agent menu is in file `/apps/agents-server/src/app/agents/[agentName]/AgentOptionsMenu.tsx`
-   You are working with the `Agents Server` application `/apps/agents-server`
-   Keep in mind the DRY _(don't repeat yourself)_ principle.

---

[-]

[✨📜] qux

-   You are working with the `Agents Server` application `/apps/agents-server`
-   Keep in mind the DRY _(don't repeat yourself)_ principle.

---

[-]

[✨📜] qux

-   You are working with the `Agents Server` application `/apps/agents-server`
-   Keep in mind the DRY _(don't repeat yourself)_ principle.

---

[-]

[✨📜] qux

-   You are working with the `Agents Server` application `/apps/agents-server`
-   Keep in mind the DRY _(don't repeat yourself)_ principle.
