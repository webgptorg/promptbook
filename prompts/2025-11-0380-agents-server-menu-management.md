[ ]

[✨🧱] In the header, there is a menu item Agents, add "Create new agent" on the bottom after "View all agents"

-   You are working with the [Header component](/apps/agents-server/src/components/Header/Header.tsx)
-   You are working with the `Agents Server` application `/apps/agents-server`
-   Keep in mind the DRY _(don't repeat yourself)_ principle.

---

[ ]

[✨🧱] Create user management

-   In the Header add menu item "Users" after "Models"
-   There shpuld be listed all users registered in the system + at the bottom "Create new user" and "View all users"
-   There should be page `/admin/users` listing all users registered in the system
-   Do not allow to create new users from main `/` page, only from `/admin/users` page
-   Each user should have page `/admin/users/[userId]` showing their profile, list of created agents, activity, etc.
-   You are working with the [Header component](/apps/agents-server/src/components/Header/Header.tsx)
-   You are working with the `Agents Server` application `/apps/agents-server`
-   Keep in mind the DRY _(don't repeat yourself)_ principle.

---

[ ]

[✨🧱] Create `/admin/models` page that will list all models available in the system

-   Link to it from the header menu
-   Limit the models on main page `/` to only 11 models, after that show "View all models" link that will lead to `/admin/models`
-   You are working with the [Header component](/apps/agents-server/src/components/Header/Header.tsx)
-   You are working with the `Agents Server` application `/apps/agents-server`
-   Keep in mind the DRY _(don't repeat yourself)_ principle.

---

[ ]

[✨🧱] All information about login should be on header menu

-   Not there is duplicated info about login both on header and on `/` page
-   The header should have all the relevant info about login status
-   You are working with the `Agents Server` application `/apps/agents-server`
-   You are working with the [Header component](/apps/agents-server/src/components/Header/Header.tsx)
-   Keep in mind the DRY _(don't repeat yourself)_ principle.

---

[ ]

[✨🧱] Create `/admin/chat-history` page that will allow viewing and managing chat history

-   It should be smart table with filters, search, pagination, etc.
-   You should be filter by agent
-   You are working with the [Header component](/apps/agents-server/src/components/Header/Header.tsx)
-   You are working with the `Agents Server` application `/apps/agents-server`
-   Keep in mind the DRY _(don't repeat yourself)_ principle.

---

[ ]

[✨🧱] Create `/admin/chat-feedback` page that will allow viewing and managing chat feedbacks

-   It should be smart table with filters, search, pagination, etc.
-   You should be filter by agent
-   You are working with the [Header component](/apps/agents-server/src/components/Header/Header.tsx)
-   You are working with the `Agents Server` application `/apps/agents-server`
-   Keep in mind the DRY _(don't repeat yourself)_ principle.

---

[ ]

[✨🧱] qux

-   You are working with the [Header component](/apps/agents-server/src/components/Header/Header.tsx)
-   You are working with the `Agents Server` application `/apps/agents-server`
-   Keep in mind the DRY _(don't repeat yourself)_ principle.

---

[ ]

[✨🧱] qux

-   You are working with the [Header component](/apps/agents-server/src/components/Header/Header.tsx)
-   You are working with the `Agents Server` application `/apps/agents-server`
-   Keep in mind the DRY _(don't repeat yourself)_ principle.

---

[ ]

[✨🧱] qux

-   You are working with the [Header component](/apps/agents-server/src/components/Header/Header.tsx)
-   You are working with the `Agents Server` application `/apps/agents-server`
-   Keep in mind the DRY _(don't repeat yourself)_ principle.

---

[ ]

[✨🧱] qux

-   You are working with the [Header component](/apps/agents-server/src/components/Header/Header.tsx)
-   You are working with the `Agents Server` application `/apps/agents-server`
-   Keep in mind the DRY _(don't repeat yourself)_ principle.
