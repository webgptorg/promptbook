[ ]

[✨🦀] Allow to change `agentName`

-   There can de discrepancy between the `agentName` and what is the derrived `agentName` from the current source
-   When this happen, in Agent page in a menu under ... show highlited link "Update URL"
-   When clicked, user should be confirmed "Are you sure you want to change the agent URL from ... to ...?"
-   If confirmed, change the agent URL to the derrived one
-   When there is no discrepancy, do not show this option
-   You are working with the `Agents Server` application `/apps/agents-server`
-   Keep in mind the DRY _(don't repeat yourself)_ principle.

---

[-]

[✨🦀] brr

-   You are working with the `Agents Server` application `/apps/agents-server`
-   Keep in mind the DRY _(don't repeat yourself)_ principle.

---

[-]

[✨🦀] brr

-   You are working with the `Agents Server` application `/apps/agents-server`
-   Keep in mind the DRY _(don't repeat yourself)_ principle.

---

[-]

[✨🦀] brr

-   You are working with the `Agents Server` application `/apps/agents-server`
-   Keep in mind the DRY _(don't repeat yourself)_ principle.
