[x]

[✨🏺] Implement Ctrl+S shortcut in <BookEditor/> component

-   When the CTRL+S is pressed in the book editor, it opens the default saving of the browser.
-   The book editor is already auto-saved, but when the user presses the CTRL+S, it should in the UI ensure him that the book is saved.
-   You are working with the `Agents Server` application `/apps/agents-server`
-   Keep in mind the DRY _(don't repeat yourself)_ principle.
-   Add the changes into the `/changelog/_current-preversion.md`

---

[x]

[✨🏺] Implement Ctrl+V shortcut in <BookEditor/> component

-   The pasting of text is already working in the book editor.
    But when a user has something else like an image in the clipboard, it doesn't work. It doesn't do anything. Implement the pasting of the files in the book editor.
-   Use the file uploading which already works in the book editor.
-   You are working with the `Agents Server` application `/apps/agents-server`
-   Keep in mind the DRY _(don't repeat yourself)_ principle, especially share the logic between the drag-and-drop of the files and pasting the files from the clipboard.
-   Add the changes into the `/changelog/_current-preversion.md`

---

[x]

[✨🏺] Implement Ctrl+S shortcut in <Chat/> component

-   When the CTRL+S is pressed in the chat component, it opens the default saving of the browser.
-   The chat has Feature that can export the chat into the multiple formats. Open this option for the user when the user presses Ctrl+S.
-   <Chat/> component is base for all other chat components like <LlmChat/>, <AgentChat/>, etc.
-   You are working with the `Agents Server` application `/apps/agents-server`
-   Keep in mind the DRY _(don't repeat yourself)_ principle.
-   Add the changes into the `/changelog/_current-preversion.md`

---

[x]

[✨🏺] Implement Ctrl+V shortcut in <Chat/> component

-   The pasting of text is already working in the chat component.
-   But when a user has something else like an image in the clipboard, it doesn't work. It doesn't do anything. Implement the **pasting of the files** in the chat component.
-   Pressing CTRL+V with files in the clipboard should behave the same as dropping the files to the chat.
-   There are file attachments in the chat component. Use the file attachments for that.
-   <Chat/> component is base for all other chat components like <LlmChat/>, <AgentChat/>, etc.
-   You are working with the `Agents Server` application `/apps/agents-server`
-   Keep in mind the DRY _(don't repeat yourself)_ principle.
-   Add the changes into the `/changelog/_current-preversion.md`

---

[-]

[✨🏺] brr

-   ...
-   You are working with the `Agents Server` application `/apps/agents-server`
-   Keep in mind the DRY _(don't repeat yourself)_ principle.
-   Add the changes into the `/changelog/_current-preversion.md`
