[ ]

[✨📱] Center search bar in Agents Server top menu

-   The search bar in the Agents Server top menu is currently shifted to the right; visually align it so it is centered in the header layout.
-   Make sure the search bar stays centered across common viewport widths (mobile, tablet, desktop) and does not jump when other header items (e.g. user menu / buttons) render.
-   Keep the overall header spacing consistent; do not change the visual size of the search input unless needed to achieve centering.
-   Prefer a layout solution (e.g. proper flex/grid structure with a dedicated center slot) over ad-hoc margins.
-   Add a quick visual check in code (storybook/screenshot/test page) only if it already exists; otherwise keep scope minimal.
-   Keep in mind the DRY _(don't repeat yourself)_ principle.
-   Do a proper analysis of the current functionality before you start implementing.
-   You are working with the [Agents Server](apps/agents-server)
    -   Likely header component/layout in [apps/agents-server/src/components/header](apps/agents-server/src/components/header)
    -   If needed, adjust related styles in the Agents Server CSS/Tailwind utilities used by the header/search components
-   No database migration needed.
-   Add the changes into the [changelog](changelog/_current-preversion.md)

---

[-]

[✨📱] bar

-   @@@
-   Keep in mind the DRY _(don't repeat yourself)_ principle.
-   Do a proper analysis of the current functionality before you start implementing.
-   You are working with the [Agents Server](apps/agents-server)
-   If you need to do the database migration, do it
-   Add the changes into the [changelog](changelog/_current-preversion.md)

---

[-]

[✨📱] bar

-   @@@
-   Keep in mind the DRY _(don't repeat yourself)_ principle.
-   Do a proper analysis of the current functionality before you start implementing.
-   You are working with the [Agents Server](apps/agents-server)
-   If you need to do the database migration, do it
-   Add the changes into the [changelog](changelog/_current-preversion.md)

---

[-]

[✨📱] bar

-   @@@
-   Keep in mind the DRY _(don't repeat yourself)_ principle.
-   Do a proper analysis of the current functionality before you start implementing.
-   You are working with the [Agents Server](apps/agents-server)
-   If you need to do the database migration, do it
-   Add the changes into the [changelog](changelog/_current-preversion.md)

---

[-]

[✨📱] bar

-   @@@
-   Keep in mind the DRY _(don't repeat yourself)_ principle.
-   Do a proper analysis of the current functionality before you start implementing.
-   You are working with the [Agents Server](apps/agents-server)
-   If you need to do the database migration, do it
-   Add the changes into the [changelog](changelog/_current-preversion.md)
